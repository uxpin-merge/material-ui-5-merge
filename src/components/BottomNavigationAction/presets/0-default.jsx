import React from 'react';
import BottomNavigationAction from '../BottomNavigationAction';
import Icon from '../../Icon/Icon';

export default (
  <BottomNavigationAction uxpId="bottom-navigation-action-1" label="Recents" showLabel icon="restore" />
);